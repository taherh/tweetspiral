o skip protected input user that we are not following

o different submit buttons instead of drop down

o sign in message on rate limit error

o setup heroku

o blog about dev
  X render vs. render_to_response
  o authenticate vs. authorize
  o tweet button url encoding

X setup oauth flow
X install twitter lib
X library for handling crunch operations
X css for page design
